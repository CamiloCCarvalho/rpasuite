# Source of truth: pyproject.toml
#   [project.dependencies]           — runtime
#   [project.optional-dependencies]  — extras (browser, opencv, ocr, dashboard, postgres, mysql, sqlserver, dev, all)
#
# Editable install with development tools:
-e ".[dev]"
